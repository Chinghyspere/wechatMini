<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ching.wechatstudy.serviceDao.SubjectDao">
<select id="querySubject" resultType="com.ching.wechatstudy.pojo.Subject">
    select * from subject where 1=1 AND subjectNo = #{subject}
</select>
</mapper>